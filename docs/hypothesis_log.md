# Hypothesis Log

Лог гипотез для задачи OOS Detection.

## Шаблон

```
### HYP-XXX: Название

- **Задача:**
- **Гипотеза:**
- **Мотивация:**
- **Реализация:**
- **Метрика:**
- **Результат:**
- **Вывод:**
- **Дата:**
```

---

## Гипотезы

### HYP-001: Per-intent threshold calibration

- **Задача:** OOS Detection
- **Гипотеза:** Индивидуальная калибровка порога для каждого интента
  (на основе плотности кластера в embedding-пространстве) улучшит
  качество OOS-детекции по сравнению с глобальным порогом.
- **Мотивация:** Разные интенты имеют разную "компактность" в
  embedding-пространстве. Глобальный порог может быть слишком
  строгим для одних интентов и слишком мягким для других.
- **Реализация:** не реализована
- **Метрика:** OOS Recall и In-Domain Accuracy на CLINC150
- **Результат:** —
- **Вывод:** Отложена. AutoML-оптимизация AutoIntent уже обеспечивает
  стабильный глобальный threshold (CV ~3%). Per-intent калибровка
  может дать прирост в сценариях с сильно неравномерной плотностью
  кластеров, но в few-shot режиме (10–20 примеров на интент)
  статистики на кластер недостаточно для надёжной калибровки.
- **Дата:** —

---

### HYP-002: Asymmetric cost function

- **Задача:** OOS Detection
- **Гипотеза:** Оптимизация порога под асимметричную функцию стоимости
  α·FNR + β·FPR (α > β) улучшит OOS Recall при приемлемой потере
  In-Domain Accuracy, так как для guardrail FN дороже FP.
- **Мотивация:** В стандартном AutoIntent threshold оптимизируется под F1,
  который симметрично взвешивает FN и FP. В guardrail-контексте
  пропущенный OOS-запрос (FN) ведёт к неверному ответу агента,
  тогда как ложно заблокированный in-scope запрос (FP) даёт
  безопасный fallback.
- **Реализация:** notebooks/05_hypothesis_asymmetric_cost.ipynb,
  постфактум подбор threshold на val-сете
- **Метрика:** OOS Recall, In-Domain Accuracy, F1 OOS, FPR
- **Результат:** 20-shot, 3 seeds (mean ± std):
  - 1:1 (baseline): Recall=0.899±0.007, Acc=0.934±0.007,
    F1=0.818±0.007, FPR=0.066±0.007
  - 2:1: Recall=0.937±0.030, Acc=0.883±0.045,
    F1=0.764±0.047, FPR=0.117±0.045
  - 5:1: Recall=0.969±0.010, Acc=0.806±0.052,
    F1=0.685±0.051, FPR=0.194±0.052
  - 10:1: Recall=0.976±0.011, Acc=0.753±0.086,
    F1=0.639±0.071, FPR=0.247±0.086
- **Вывод:** Опровергнута в части "без критической потери In-Domain Acc".
  Механизм работает, но trade-off невыгоден: F1-оптимизированный
  baseline (1:1) уже достигает Recall=0.899 при FPR=0.066.
  Переход к 2:1 нестабилен (std Acc=0.045). При α≥5 каждый
  пятый легитимный запрос блокируется. Асимметричная cost function —
  configurable knob для специфических требований, но не улучшение
  пайплайна по умолчанию.
- **Дата:** апрель 2026

---

## Открытые вопросы к авторам AutoIntent

Вопросы, влияющие на интерпретацию результатов и воспроизводимость.
Направить на: ilya_alekseev_2016@list.ru (Alekseev Ilya, EMNLP 2025)

### Q1: Конфигурация CLINC150 в Table 3

Какая конфигурация датасета использована: `full` (100 OOS train)
или `plus` (250 OOS train, как в `DeepPavlov/clinc150`)?

Влияние: если `DeepPavlov/clinc150` — наши результаты (F1=0.841)
сравнимы с Table 3 (F1=0.768), расхождение объясняется версией
библиотеки. Если `full` — нужен перезапуск на `clinc/clinc_oos`.

### Q2: Публичный API для continuous OOS-скоров

`pipeline.predict()` возвращает только бинарные предсказания,
что делает AUROC несравнимым с cosine-бейзлайнами.

Мы получаем continuous скоры через `pipeline.predict_with_metadata()`:
```python
results = pipeline.predict_with_metadata(texts)
oos_scores = 1 - np.array([u.score for u in results.utterances]).max(axis=1)
```
Это официально поддерживаемый способ или внутренний API,
который может измениться в следующих версиях?

### Q3: Аномалия качества на 50-shot

Наблюдается немонотонное поведение F1 OOS:
10-shot=0.724 → 20-shot=0.819 → **50-shot=0.730** (провал) → full=0.841.

Анализ Optuna-логов показывает что архитектура стабильна
(linear + threshold во всех 9 runs), но val/test gap растёт:
+0.073 при 20-shot vs +0.218 при 50-shot — признак переобучения
порога на val-фолдах при CV.

Это известное ограничение TunableDecision? Есть ли рекомендации
по выбору n_folds или стратегии валидации в few-shot режиме?

### Q4: Embedder в Table 3

Мы фиксировали `multilingual-e5-large-instruct` явно через
`EmbedderConfig`. Preset `classic-light` без фиксации запускает
pipeline-level оптимизацию embedder'а и может выбрать другую модель
(по Table 5 лучший — `stella_en_400M_v5` с accuracy 94.28 vs 91.41).

Был ли embedder зафиксирован в экспериментах Table 3,
или выбирался автоматически?

---

## Jailbreak Detection

### HYP-JB-001: Обобщение OOS-пайплайна на adversarial данные

- **Задача:** Jailbreak Detection
- **Гипотеза:** AutoIntent с OOS-переформулировкой (safe = in-scope,
  jailbreak = OOS) воспроизводит качество OOS-спринта на задаче
  jailbreak detection
- **Мотивация:** jailbreak detection структурно аналогична OOS-детекции,
  но данные adversarial по природе — неизвестно, ухудшится ли качество
- **Реализация:** —
- **Метрика:** F1 на jailbreak-классе, Over-refusal Rate
- **Результат:** —
- **Вывод:** —
- **Статус:** запланирована

---

### HYP-JB-002: Vanilla vs Adversarial — generalization gap

- **Задача:** Jailbreak Detection
- **Гипотеза:** AutoIntent теряет качество на adversarial промптах
  относительно vanilla
- **Мотивация:** adversarial промпты маскируют вредоносный intent,
  что может обмануть embedding-based классификатор
- **Реализация:** —
- **Метрика:** F1 отдельно на vanilla_harmful и adversarial_harmful
  подмножествах eval
- **Результат:** —
- **Вывод:** —
- **Статус:** запланирована

---

### HYP-JB-003: AutoIntent как бинарный классификатор на adversarial данных

- **Задача:** Jailbreak Detection
- **Гипотеза:** AutoIntent в режиме бинарной классификации (safe=0,
  jailbreak=1) достигает конкурентного качества на adversarial
  промптах WildJailbreak в few-shot режиме
- **Мотивация:** OOS-переформулировка невозможна — scoring-модуль
  требует 2+ класса. Бинарная постановка более естественна для
  jailbreak: у нас есть явные примеры обоих классов, adversarial
  промпты — не случайные аномалии, а структурированные атаки
  с распознаваемыми паттернами
- **Реализация:** tasks/jailbreak_detection/notebooks/03_autointent_fewshot.ipynb,
  n_shots: [10, 20, 50], seeds: [42, 123, 456], 9 runs
- **Метрика:** F1, Recall, Over-refusal Rate на eval WildJailbreak (2210 примеров)
- **Результат:** 10-shot: F1=0.757±0.026, Recall=0.642±0.039;
  20-shot: F1=0.796±0.062, Recall=0.701±0.108;
  50-shot: F1=0.731±0.148, Recall=0.623±0.196 (CV=20.2%, UNSTABLE).
  Лучший run: 20-shot seed=42, F1=0.867, Recall=0.825,
  Over-refusal Rate=0.743.
- **Вывод:** Частично подтверждена. AutoIntent как бинарный
  классификатор работает на adversarial данных (F1≈0.796 при 20-shot,
  сопоставимо с OOS-спринтом F1=0.819). Критическая проблема:
  высокий Over-refusal Rate (до 74% на лучшем run) и нестабильность
  на 50-shot (CV=20.2%). Скейлинг немонотонен — та же аномалия,
  что в OOS-спринте.
- **Статус:** выполнена

---

### HYP-JB-004: Асимметричная cost function для снижения Over-refusal Rate

- **Задача:** Jailbreak Detection
- **Гипотеза:** Постфактум подбор порога под асимметричную функцию
  α·FNR + β·FPR (β > α, FP дороже FN) снизит Over-refusal Rate
  до приемлемого уровня (<20%) при умеренной потере Recall
- **Мотивация:** Лучший run (20-shot seed=42) блокирует 74% безопасных
  промптов — неприемлемо для guardrail. В отличие от OOS-спринта
  (HYP-002), где baseline уже имел низкий FPR=0.066 и асимметрия
  давала невыгодный trade-off, здесь стартовый Over-refusal Rate
  критически высок — есть пространство для улучшения
- **Реализация:** —
- **Метрика:** Over-refusal Rate, Recall, F1 на eval WildJailbreak
- **Результат:** —
- **Вывод:** —
- **Статус:** запланирована
